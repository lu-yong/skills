interface:
  display_name: "Refresh BU1-SDK Rules"
  short_description: "Refresh local BU1-SDK rule snapshots"
  default_prompt: "Use $refresh-bu1-sdk-rules to fetch, review, and explicitly confirm updates to the local BU1-SDK rule snapshots."
policy:
  allow_implicit_invocation: false
